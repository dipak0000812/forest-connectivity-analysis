# Dependencies for Python validation module
# Note: GEE is the primary platform, Python is for validation only

numpy>=1.24.0
scipy>=1.10.0
rasterio>=1.3.0
geopandas>=0.13.0
shapely>=2.0.0
pytest>=7.0.0